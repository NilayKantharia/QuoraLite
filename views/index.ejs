<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <!-- <link href="/public/style.css" rel="stylesheet" type="text/css"> -->
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Post</h1>
    <% posts.forEach(post => { %>
     <div class="post">
        <h3 class="user">@<%= post.username %></h3>
        <h4 class="content"><%= post.content %></h4>
        <a href="http://localhost:8000/posts/<%= post.id %>">See full post</a>
        <a href="http://localhost:8000/posts/<%= post.id %>/edit">Edit</a>
        <form action="/post/<%= post.id %>?_method=DELETE" method="post">
            <button type="submit">Delete post</button>
        </form>
     </div>
    <% }) %>
    <a href="http://localhost:8000/posts/new">Create a new post</a>

</body>
</html>